$(function () {
   $('#hourly').highcharts('StockChart', {
   
          chart: {
             renderTo: 'hourly'
          },
           rangeSelector : {
                selected : 1,
                inputEnabled: $('#hourly').width() > 480
            },

            title : {
                text : 'Weather graph'
            },
            xAxis: {
                title:{
                    text: "Datetime"
                },
                type: 'datetime',
                
            },
            yAxis: {
                title:{
                    text: "Temperature"
                }
            },
            tooltip: {
                enabled: true,
                useHTML: true,
                delayForDisplay: 1000,
                hideDelay: 1,
                borderWidth: 0
            },
           
            plotOptions: {
                
            },
          series: [{
            name: "Temp",
            temp: "Temperature",
            data: hourly_data ,
            
          }]
    });
});